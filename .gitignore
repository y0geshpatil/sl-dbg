# Binaries
/bin/
/dist/
/sl-dbg
/sl-dbgd

# Compiled test binaries only (Go's default naming for `go test -c`)
*.test
*.out
*.prof
coverage.*

# IDE / OS
.idea/
.vscode/
.DS_Store
*.swp

# Local config / state
/.local/
*.log
*.sock

# Adapter cache (auto-downloaded jars/binaries)
/.cache/

# Vendor (we use modules; vendor only if explicitly enabled)
/vendor/
__debug_bin*

# Java launcher build output
adapters/java-launcher/target/

dist/
